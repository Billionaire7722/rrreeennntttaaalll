# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Docker Network Connection Failures
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: Focus on concrete failing cases - frontend using host network URLs, backend using localhost for databases
  - Test that frontend connections fail when using `http://{VPS_HOSTNAME}:3000` in Docker production
  - Test that backend connections fail when using `DATABASE_URL` with `localhost:5433` in Docker production
  - Test that backend connections fail when using `REDIS_HOST=localhost` in Docker production
  - The test assertions should verify connection failures match observed symptoms (connection refused, timeout, "cannot connect" errors)
  - Run test on UNFIXED code in Docker production environment
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found:
    - Frontend connection attempt to `http://103.200.22.111:3000` fails
    - Backend PostgreSQL connection to `localhost:5433` fails
    - Backend Redis connection to `localhost:6379` fails
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6_

- [ ] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Local Development and Existing Functionality Preserved
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (local development environment)
  - Test local development connections using `http://127.0.0.1:3000` or `http://localhost:3000`
  - Test backend connects to PostgreSQL on `localhost:5433` in local development
  - Test backend connects to Redis on `localhost:6379` in local development
  - Test authenticated requests with valid JWT tokens are processed correctly
  - Test CORS configuration with `CORS_ORIGIN=*` allows cross-origin requests
  - Test Docker build process completes successfully
  - Test Prisma migrations run successfully via init container
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [ ] 3. Fix Docker network configuration for production deployment

  - [ ] 3.1 Update backend environment configuration for production
    - Create or update `backend/.env.production` file
    - Set `DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/rental?schema=public`
    - Set `REDIS_HOST=redis`
    - Set `REDIS_PORT=6379`
    - Ensure `CORS_ORIGIN` is set appropriately for production
    - Keep existing `backend/.env` or `backend/.env.local` for local development with localhost URLs
    - _Bug_Condition: isBugCondition(deployment) where deployment.component == "backend" AND (deployment.databaseUrl.contains("localhost") OR deployment.redisHost == "localhost")_
    - _Expected_Behavior: Backend uses Docker service names (postgres:5432, redis:6379) in production_
    - _Preservation: Local development continues to use localhost:5433 and localhost:6379_
    - _Requirements: 1.2, 1.3, 2.2, 2.3, 3.3_

  - [ ] 3.2 Update frontend environment configuration for Next.js users app
    - Create or update `users/.env.production` file
    - Set `NEXT_PUBLIC_API_BASE_URL` to public backend URL (e.g., `http://103.200.22.111:3000` or domain)
    - Update Next.js API client code to use `NEXT_PUBLIC_API_BASE_URL` for client-side requests
    - Update Next.js server-side code (SSR, API routes) to use `http://backend:3000` for internal Docker network requests
    - Keep existing `users/.env` or `users/.env.local` for local development with localhost URLs
    - _Bug_Condition: isBugCondition(deployment) where deployment.component == "frontend" AND deployment.backendUrl.contains(deployment.vpsHostname)_
    - _Expected_Behavior: Frontend uses http://backend:3000 for server-side requests and public URL for client-side requests_
    - _Preservation: Local development continues to use http://127.0.0.1:3000_
    - _Requirements: 1.1, 1.4, 2.1, 2.4, 3.1_

  - [ ] 3.3 Update frontend environment configuration for Vite super-admin dashboard
    - Create or update `super-admin/.env.production` file
    - Set `VITE_API_BASE_URL` to public backend URL (e.g., `http://103.200.22.111:3000` or domain)
    - Update Vite API client code to use `VITE_API_BASE_URL` for all requests
    - If using Vite proxy in production, configure it to use `http://backend:3000`
    - Keep existing `super-admin/.env` or `super-admin/.env.local` for local development with localhost URLs
    - _Bug_Condition: isBugCondition(deployment) where deployment.component == "frontend" AND deployment.backendUrl.contains(deployment.vpsHostname)_
    - _Expected_Behavior: Frontend uses public URL for client-side requests (or http://backend:3000 for proxied requests)_
    - _Preservation: Local development continues to use http://127.0.0.1:3000_
    - _Requirements: 1.1, 1.4, 2.1, 2.4, 2.7, 3.1_

  - [ ] 3.4 Update Docker Compose configuration for production
    - Review `docker-compose.yml` (production configuration)
    - Ensure backend service has environment variables from `.env.production`
    - Ensure frontend services (users, super-admin) have environment variables from their respective `.env.production` files
    - Verify service names match configuration (backend, postgres, redis)
    - Verify network configuration allows inter-service communication
    - Ensure prisma-migrate init container uses correct `DATABASE_URL` with `postgres:5432`
    - _Bug_Condition: Docker services using localhost instead of service names_
    - _Expected_Behavior: All services use Docker service names for internal communication_
    - _Preservation: docker-compose.local.yml remains unchanged for local development_
    - _Requirements: 1.2, 1.3, 2.2, 2.3, 3.5, 3.6_

  - [ ] 3.5 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Docker Network Connection Successes
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1 in Docker production environment
    - Verify frontend can connect to backend using Docker service names
    - Verify backend can connect to PostgreSQL using `postgres:5432`
    - Verify backend can connect to Redis using `redis:6379`
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 2.7_

  - [ ] 3.6 Verify preservation tests still pass
    - **Property 2: Preservation** - Local Development and Existing Functionality Preserved
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - Verify local development connections still work with localhost URLs
    - Verify authenticated request processing unchanged
    - Verify CORS behavior unchanged
    - Verify Docker build process unchanged
    - Verify Prisma migrations still work
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [ ] 4. Checkpoint - Ensure all tests pass
  - Run all tests in both local development and Docker production environments
  - Verify frontend applications can connect to backend in production
  - Verify backend can connect to PostgreSQL and Redis in production
  - Verify users can log in and sign up successfully
  - Verify data loads consistently without appearing/disappearing
  - Verify super-admin dashboard connects successfully
  - Verify local development environment still works as before
  - If any issues arise, document them and ask the user for guidance
